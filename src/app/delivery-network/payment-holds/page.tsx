import {AppShell} from '@/components/app-shell';import {WorkforceHoldDesk} from '@/components/workforce-hold-desk';import {requirePagePermission,hasPermission,isCompanyOwner} from '@/lib/authorization';import {manageHold} from './actions';
export const dynamic='force-dynamic';
export default async function PaymentHolds({searchParams:params={}}:{searchParams?:{status?:string;notice?:string;error?:string}}){const auth=await requirePagePermission('workforce_adjustments','access');return <AppShell active="Payment Holds" pageCode="workforce_adjustments">{params.notice||params.error?<p role="status">{params.error||params.notice}</p>:null}<WorkforceHoldDesk auth={auth} action={manageHold} canEdit={hasPermission(auth,'workforce_adjustments','edit')} reviewer={isCompanyOwner(auth)} status={params.status}/></AppShell>;}
